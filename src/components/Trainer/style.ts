import styled from "styled-components"
import { Button } from "antd"

export const Trainer = styled(Button)`
  margin: 0 1rem;
  height: 3rem;
  background-color: lightgrey;
`
export const AddTrainerButton = styled(Button)`
  margin: 0 1rem;
  height: 3rem;
`
export const TrainerMembers = styled.div`
  margin: 2rem 0;
`
